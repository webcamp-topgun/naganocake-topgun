<h1>商品詳細</h1>

<%= image_tag Item.image, size: "200x200" %>

<table>
  <tr>
    <td>商品名</td>
    <td><%= @item.name %></td>
  </tr>
  <tr>
    <td>商品説明<td>
    <td><%= @item.caption %></td>
  </tr>
  <tr>
    <td>ジャンル</td>
    <td><%= @item.genre.name %></td>
  </tr>
  <tr>
    <td>税込み価格<br>
        （税抜き価格）
    </td>
    <td><%= @tax_include_price %><%= (@item.price) %>円</td>
  </tr>
  <tr>
    <td>販売ステータス</td>
    <% if @item.is_active %>
      <td>販売中</td>
    <% else %>
      <td>販売停止中</td>
    <% end %>
  </tr>
</table>

